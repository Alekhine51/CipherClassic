include <stdio.h>

// Monoalphabetic substitution cipher

int main (void) {

	int mod; //shift
	char alphabet[25];
	
	char str[length] = text //store plaintext
	
	printf("Enter modulus:");
	scanf("%d", mod);

	printf("Enter plaintext:")
	scanf("%c", text)
}
